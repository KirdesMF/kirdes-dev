export interface CardData {
	id: string;
	title: string;
	category: string;
	description: string;
	imageUrl: string;
	color: string;
}

export interface CurveConfig {
	amplitude: number; // How deep the curve dips
}
